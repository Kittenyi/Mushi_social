PORT=5001
MONGODB_URI=mongodb://localhost:27017/mushi_social
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=30d

# Client URL
CLIENT_URL=http://localhost:5173

# 身份矿工 (Soul 标签，连接钱包后分析)
# Web3.bio 可选，无 key 也可用（有限流）https://api.web3.bio/
# WEB3_BIO_API_KEY=
# Alchemy 可选，用于 txCount + NFT + 账户年龄（无 key 时 txCount=0, nfts=[]）
# ALCHEMY_API_KEY=
# Tally 可选，用于 DAO Governor 提案数 https://docs.tally.xyz/tally-api/welcome
# TALLY_API_KEY=
